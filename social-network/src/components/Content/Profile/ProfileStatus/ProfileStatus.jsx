import React from "react"
import classes from "./ProfileStatus.module.css"

const ProfileStatus = (props) => {
    return (
        <div className={classes.profileStatusContainer}> Статус </div>
    )
};
export default ProfileStatus
